services:
  api:
    build:
      dockerfile: ./deploy/Dockerfile
      context: .
    volumes:
      - "./settings.yaml:/code/settings.yaml:ro" # Read-only settings file
    restart: always
    ports:
      - "8000:8000"
    env_file: .env
